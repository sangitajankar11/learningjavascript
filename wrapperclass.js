//Autoboxing - string is primitive and it is putting it into string object box
//Auto unboxing - string object is unboxed to primitive string when we are using it as a primitive string. 
// This is done automatically by JavaScript when we are using the string as a primitive string. 
// This is done to provide us with the convenience of using string methods on primitive strings without having to manually create string objects.

//while doing strict comparison, it will not do type coercion and will return false if the types are different. 
// autoboxing and auto unboxing will not happen in strict comparison because the types are different. 
// In non-strict comparison, it will do type coercion and will return true if the values are equal after type coercion.